■ローカルリポジトリに登録
リポジトリを初期化
git init

ステージ領域へファイルを追加
git add ファイル名

リポジトリの歴史を記録
git commit -m "コメント"

■リモートリポジトリに登録（ローカルリポジトリに登録後）

リモートの登録（一度登録しておけば不要）
git remote add リモート名 リモートのURL

リモートにファイルをアップロードする
git push origin master
※originはリモートの登録の際に使用したリモート名
